version: '3'

services:
  alpine-client:
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - guacnetwork
    ports:
      - "5900:5900"
      - "2234-2239:2234-2239"  # Default Nicotine+ ports

networks:
  guacnetwork:

volumes:
  postgres-data:
  guacamole_config:
